{
  "name": "uno-game",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Uno multiplayer (Cloudflare Workers + WebSocket)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node tests/sim.mjs && node tests/server-e2e.mjs",
    "cards": "python3 tools/generate_cards.py"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
